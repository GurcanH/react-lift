export class LiftModel {
  constructor(at, travelTo, direction) {
    this.at = at; // at which floor
    this.travelTo = travelTo; // to which floor
    this.direction = direction; // to which way
  }
}
